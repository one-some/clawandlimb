name: Godot Export Release

on:
  push:
    branches:
      - master

env:
  GODOT_VERSION: 4.4.1
  EXPORT_NAME: ClawAndLimb

jobs:
  export-builds:
    name: Export and Release
    runs-on: ubuntu-22.04

    strategy:
      matrix:
        include:
          - platform: windows
            export_preset: "Windows Desktop"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Get short commit SHA
        id: get_sha
        run: echo "SHA=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Set up Godot
        uses: abarichello/godot-ci@v4
        with:
          godot-version: ${{ env.GODOT_VERSION }}
          mono: false

      - name: Build Project
        run: |
          mkdir -p build/${{ matrix.platform }}
          godot --headless --verbose --export-release "${{ matrix.export_preset }}" "build/${{ matrix.platform }}/${{ env.EXPORT_NAME }}.exe"

      - name: Zip Artifacts
        run: |
          cd build/${{ matrix.platform }}
          zip -r ../${{ env.EXPORT_NAME }}-${{ matrix.platform }}-${{ steps.get_sha.outputs.SHA }}.zip .

      - name: Update Nightly Release
        uses: ncipollo/release-action@v1
        with:
          tag: nightly
          allowUpdates: true
          replacesArtifacts: true
          artifacts: "build/${{ env.EXPORT_NAME }}-${{ matrix.platform }}-${{ steps.get_sha.outputs.SHA }}.zip"
          token: ${{ secrets.GITHUB_TOKEN }}
